import { motion } from 'motion/react'
import Reveal from './Reveal'
function ExamVisual() { return <div className="system-visual system-visual--exam" aria-label="Exam registration system flow diagram"><div className="system-node">Students</div><div className="system-node">Instructors</div><div className="system-core">Flask<br /><small>Role-based workflows</small></div><div className="system-node system-node--database">MySQL</div><svg viewBox="0 0 600 360" aria-hidden="true"><path d="M144 90C240 90 222 150 300 150M144 270C240 270 222 210 300 210M390 180H468" /><circle cx="300" cy="180" r="4" /></svg></div> }
function RedisVisual() { return <div className="system-visual system-visual--redis" aria-label="Redis-like network service flow diagram"><div className="terminal-line"><span>client</span><code>PING</code></div><div className="redis-core">TCP<br /><small>multi-client service</small></div><div className="terminal-line terminal-line--right"><code>JSON</code><span>persistence</span></div><div className="redis-commands"><code>SET</code><code>GET</code><code>DEL</code></div></div> }
export default function ProjectShowcase({ project, index }) { return <article className={`project project--${project.variant}${index === 0 ? ' project--primary' : ''}`}><Reveal className="project-copy" delay={.04}><div className="project-overline"><span>{String(index + 1).padStart(2, '0')}</span>{project.status && <span className="project-status"><i aria-hidden="true" />{project.status}</span>}</div><h3>{project.title}</h3><p className="project-stack">{project.stack.join(' · ')}</p>{project.context && <p className="project-context">{project.context}</p>}<p className="project-description">{project.description}</p><ul className="project-details">{project.details.map(detail => <li key={detail}>{detail}</li>)}</ul></Reveal><Reveal className="project-media" delay={.12}><motion.div className="project-media-inner" whileHover={{ y: -5, scale: 1.01 }} transition={{ type: 'spring', stiffness: 220, damping: 22 }}>{project.variant === 'exam' ? <ExamVisual /> : <RedisVisual />}</motion.div></Reveal></article> }
