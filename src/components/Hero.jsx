import { motion, useReducedMotion, useScroll, useTransform } from 'motion/react'
import { useRef } from 'react'
export default function Hero() {
  const heroRef = useRef(null); const reduced = useReducedMotion(); const { scrollYProgress } = useScroll({ target: heroRef, offset: ['start start', 'end start'] }); const titleY = useTransform(scrollYProgress, [0, 1], ['0%', reduced ? '0%' : '-14%']); const titleScale = useTransform(scrollYProgress, [0, 1], [1, reduced ? 1 : .9]); const titleOpacity = useTransform(scrollYProgress, [0, .82], [1, reduced ? 1 : 0]); const terrainY = useTransform(scrollYProgress, [0, 1], ['0%', reduced ? '0%' : '12%']); const enter = (y) => reduced ? false : { opacity: 0, y }
  return <section id="top" ref={heroRef} className="hero" aria-labelledby="hero-title"><motion.div className="hero-terrain" style={{ y: terrainY }} aria-hidden="true"><span /><span /><span /></motion.div><motion.div className="hero-content" style={{ y: titleY, scale: titleScale, opacity: titleOpacity }}><motion.h1 id="hero-title" initial={enter(28)} animate={{ opacity: 1, y: 0 }} transition={{ duration: .8, ease: [.22, 1, .36, 1] }}>Giovanni Peila</motion.h1><motion.p className="hero-role" initial={enter(20)} animate={{ opacity: 1, y: 0 }} transition={{ duration: .7, delay: .12, ease: [.22, 1, .36, 1] }}>Computer Science @ Cal Poly SLO</motion.p><motion.p className="hero-summary" initial={enter(18)} animate={{ opacity: 1, y: 0 }} transition={{ duration: .7, delay: .22, ease: [.22, 1, .36, 1] }}>Building software and exploring AI, systems, and the web.</motion.p><motion.div className="hero-actions" initial={enter(16)} animate={{ opacity: 1, y: 0 }} transition={{ duration: .65, delay: .32, ease: [.22, 1, .36, 1] }}><a className="button button--primary" href="#work">View selected work <span aria-hidden="true">↓</span></a><a className="button button--quiet" href="https://github.com/Giovanni-67" target="_blank" rel="noreferrer">GitHub <span aria-hidden="true">↗</span></a><a className="button button--quiet" href="#contact">Contact <span aria-hidden="true">↘</span></a></motion.div></motion.div><a className="hero-scroll-cue" href="#work" aria-label="Scroll to selected work"><span>SCROLL TO EXPLORE</span><i aria-hidden="true" /></a></section>
}
